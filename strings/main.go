package main

import (
	// "fmt"
	// "strings"
)

func main() {

	// data := "apple.orange.banana"
	// parts := strings.Split(data,".")
	// fmt.Println(parts)

	// str := "one two three one two two two two"
	// count := strings.Count(str,"two")
	// fmt.Println(count)

	// str := "  Hello  Go ! "
	// fmt.Println(str)
	// trimmed := strings.TrimSpace(str)
	// fmt.Println(trimmed)

	// str := "Prince"
	// str2 := "Aggarwal"
	// result := strings.Join([]string{str,str2}," ")
	// fmt.Println(result)
}